# COMMANDES DE RÉORGANISATION DES FICHIERS

# 1. DÉPLACEMENT DES FICHIERS DE DONNÉES BRUTES
mv espn_*.json data/raw/
mv espn_*.csv data/raw/
mv espn_free.log logs/

# 2. DÉPLACEMENT DES SCRIPTS DE COLLECTE
mv collect_data.py src/collectors/
mv extract_*.py scripts/
mv realtime_collector.py scripts/
mv run_*.py scripts/

# 3. DÉPLACEMENT DES ANALYSEURS
mv *_analyzer.py src/processors/
mv analyze_raw_data.py src/processors/
mv advanced_analysis_sheets.py src/processors/

# 4. DÉPLACEMENT DES FICHIERS DE CONFIGURATION
mv requirements*.txt config/
mv setup_instructions.md config/

# 5. DÉPLACEMENT DES TESTS
mv test_*.py tests/

# 6. DÉPLACEMENT DE LA DOCUMENTATION
mv README*.md docs/

# 7. DÉPLACEMENT DES UTILITAIRES
mv debug_espn.py src/utils/
mv find_league_id.py src/utils/

# 8. NETTOYAGE
rm -rf __pycache__
rm -f *.pyc
rm -f .DS_Store

# 9. VÉRIFICATION
ls -R
